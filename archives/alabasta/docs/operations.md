# Operations Handbook

The Alabasta service is expected to be monitored by standard Navigation Network tooling.

Runtime logs are emitted at the configured `ALABASTA_LOG` level. The default logging level is `info`.

Operational behavior may vary depending on cached runtime paths and preserved legacy settings.
