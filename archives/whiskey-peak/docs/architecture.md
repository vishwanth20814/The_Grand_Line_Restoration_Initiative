# Whiskey Peak Architecture

The Whiskey Peak service reconciles station state and compatibility information from the Navigation Network runtime environment.

This version was handed off from Reverse Mountain after runtime stabilization.

The current service resolves asset and cache directory paths from two different base locations.
