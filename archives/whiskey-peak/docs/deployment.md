# Deployment Notes

Launch from repository root and ensure `config/application.toml` is present. The service uses the current working directory to resolve the cache path.

Legacy mode is intentionally preserved to support older downstream transition processes.
