[package]
name = "reverse-mountain"
version = "0.1.0"
edition = "2021"
description = "Reverse Mountain migration service for Navigation Network runtime reconciliation."
authors = ["Reverse Mountain Migration Team <migration@navnet.local>"]
license = "Apache-2.0"
repository = "https://internal.navnet.local/repos/reverse-mountain"
readme = "README.md"

[dependencies]
anyhow = "1.0"
env_logger = "0.10"
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.7"
thiserror = "1.0"
