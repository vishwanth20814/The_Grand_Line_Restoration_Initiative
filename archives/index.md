# Archives Index

This directory contains recovered Navigation Network archives at varying restoration states:

- `east-blue/` — Complete archive with runtime code, config, docs, and logs.
- `reverse-mountain/` — Partial archive with runtime components and recovery artifacts.
- `whiskey-peak/` — Metadata-rich archive preserving Git history, release notes, and deployment metadata.
- `alabasta/` — Final archive manifest with production deployment artifacts and integration validation.
